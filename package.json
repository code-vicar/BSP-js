{
  "name": "@code-vicar/bsp",
  "version": "1.0.3",
  "description": "Binary space partitioned graph generator",
  "main": "lib/index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "npm run compile && npm run bundle",
    "compile": "rm -rf lib && babel src -d lib",
    "bundle": "rm -rf bundle && webpack --mode=production lib/index.js -o ./bundle/bsp-js.js"
  },
  "author": "Scott Vickers",
  "license": "Apache-2.0",
  "dependencies": {
    "@code-vicar/graphlib": "^1.2.0",
    "debug": "^4.1.0",
    "lodash.shuffle": "^4.2.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.5",
    "@babel/core": "^7.1.6",
    "@babel/preset-env": "^7.1.6",
    "@babel/register": "^7.0.0",
    "webpack": "^4.25.1",
    "webpack-cli": "^3.1.2"
  }
}
